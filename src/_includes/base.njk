<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title | safe }} - Re Alvarez Parmar's personal site</title>
  <link rel="stylesheet" href="/styles.css" />
  <meta property="og:title" content="{{ title | safe }} - Re Alvarez Parmar's personal site" />
  <meta property="og:description" content="Write here..." />
  <meta property="og:url" content="https://github.com/CLCK0622/11ty-Serene" />
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
  <script src="/main.js"></script>
</head>
<body class="main-body">

  <div class="box nav-box">
    <nav class="nav-bar">
      <div class="nav-title">Re Alvarez Parmar's personal site</div>
      <div class="nav-links">
        {% for page in collections.pages %}
          <a href="{{ page.url }}" class="nav-link">{{ page.data.title }}</a>
        {% endfor %}
      </div>
      <span class="dark-toggle">
        <span class="material-symbols-outlined">light_mode</span>
        <label class="switch">
          <input
            type="checkbox"
            id="darkmode"
            onclick="change_mode()"
            aria-labelledby="darkmodelabel"
          />
          <span class="slider round"></span>
        </label>
        <span class="material-symbols-outlined">dark_mode</span>
      </span>
    </nav>
  </div>

  <div class="content-wrapper">
    <div class="box">{{ content | obsidianImages | safe }}</div>

    <div class="box footer footer-box">
      
      <p class="footer-text">© 2025 Re Alvarez Parmar</a></p>
    </div>
  </div>
</body>
</html>
